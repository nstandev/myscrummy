import { AppTouchGoalDirective } from './app-touch-goal.directive';

describe('AppTouchGoalDirective', () => {
  it('should create an instance', () => {
    const directive = new AppTouchGoalDirective();
    expect(directive).toBeTruthy();
  });
});
